using System.Windows;

namespace AI_Group
{
    /// <summary>
    /// App.xaml 的交互逻辑
    /// </summary>
    public partial class App : Application
    {
        //protected override void OnStartup(StartupEventArgs e)
        //{
        //    base.OnStartup(e);
        //    var mainWindow = new MainWindow();
        //    mainWindow.Show();   // 先创建并显示窗口（确保窗口句柄生成）
        //    mainWindow.Hide();   // 立即隐藏，避免闪烁
        //}
    }
}
